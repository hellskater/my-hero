import React from 'react';
import { ProfileForm } from './_client/profile-form';

const AcccountPage = () => {
  return (
    <section>
      <ProfileForm />
    </section>
  );
};

export default AcccountPage;
